#include <iostream>

using namespace std;

int main()
{
    int x = 2; // x is variable, 2 is a literal.

    // 3항 연산자. (x > 0) 이 true 면 1, false 면 2
    int y = (x > 0) ? 1 : 2; 

    cout << x + 2 << endl;
    cout << "My Home" << endl;

    return 0;
}